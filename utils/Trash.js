   
   //console.log( 'UseEffect: time difference is ', timeDifference);
  //console.log('tempElapsedTime', tempElapsedTime);
  //console.log('totalElapsedTime', totalElapsedTime);
   
   /*
        console.log('timedifference is ', timeDifference);
        let differenceDate = new Date(timeDifference * 1000);
        let diffHours = differenceDate.getUTCHours();
        console.log('diffHours', diffHours);
        let diffMinutes = differenceDate.getUTCMinutes();
        console.log('diffMinutes', diffMinutes);
        let diffSeconds = differenceDate.getUTCSeconds();
        console.log('diffSeconds', diffSeconds);
        let readableDifference = diffHours + ':' + diffMinutes + ':' + diffSeconds;
        console.log('readableDifference', readableDifference);
        */